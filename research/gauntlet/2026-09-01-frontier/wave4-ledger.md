# Wave 4 ledger — frontier gauntlet 2026-09-01

One repair owner per direction applied both wave-3 reviewers' highest-impact fixes (identification, decision rules, provenance corrections, licence checks), re-ran the validator (9/9 PASS) and the doctor (FAIL by construction: no evidence bundle, Compute FAIL, unsigned same-provider reviews), then two fresh reviewers scored. Lower total is authoritative; caps 74/79/89 remain live. **Three of four scores fell**; the dips are recorded, not smoothed.

| direction | wave 3 → wave 4 | fixes applied | reviewers' next highest-impact fix |
|---|---:|---:|---|
| `icl-rule-distillation-port` | 57 → **65** | 9 | Make Stage A executable and attested, and sync the contract to the repository state in the same versioned pass. Concretely: (1) implement the CPU dMMSE-regime synthetic d // Defect: the attribution tree's decision power is mis-stated. C25 and Evaluation report power 0.78 for the primary leaf D(R_theta, R_gf) alone, but P1 CONFIRMED is a conju … |
| `semantic-clock-gate-parity` | 64 → **63** | 10 | Make the primary estimand headroom-invariant and add a within-tokenizer warp reference in phase 0. Concretely: (1) promote the logit-scale (or, equivalently, matched-base // Add a training-free synthetic-fertility English positive control to pass B as a never-cut cell: for each non-English f_L, re-segment the English fact block and NTREX dist … |
| `translation-supervised-sparse-indexer` | 66 → **62** | 7 | Re-derive the Phase-1 decision rule from a measured, fully specified noise model and separate the gate prompts from the primary prompts, as one versioned re-registration: // Register a lambda_x selection step that actually exists and is budgeted: add to Phase 1 a pre-step training (T*)+L_x at both lambda_x values on development languages only … |
| `translation-equivariant-state-writes` | 63 → **60** | 10 | Make the decisive contrast's interpretability gate measured instead of assumed, at zero GPU cost: implement the CPU object doctor as real code (pure-PyTorch 2-layer GDN r // Re-derive the promotion and kill rule from the actual decision and justify it by simulation, per the K-Dense statistical-power protocol the proposal cites. Concretely: pr … |

Convergent finding: every reviewer's binding defect is now cap-class — **no executable pilot exists** (cap 79): the CPU phase-0 doctors, noise-model measurements, and hyperparameter-selection steps are specified in prose but not implemented. Wave 5 therefore implements the CPU doctors as tested code wired into each contract's `reference_doctor`.

Score trajectories (lower reviewer total): sparse-indexer 61 → 66 → 62; clock-parity 61 → 64 → 63; state-writes 57 → 63 → 60; icl-rule 62 → 57 → 65. Best-so-far 66 (wave 3). Full records: `wave4-result.json`.
