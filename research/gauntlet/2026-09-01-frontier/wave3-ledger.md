# Wave 3 ledger — frontier gauntlet 2026-09-01

Each wave-2 survivor received one spec owner who applied the two wave-2 judges' shared highest-impact identification fix (one repair per wave) and wrote the proposal, the experiment contract (all pass `validate_architecture_experiments.py`, `execution.enabled: false`), and a direction file; two fresh reviewers then scored each proposal on the gauntlet rubric and filled the ARS criterion-bound form. Scores are the lower of two totals, capped at 89 (no provider-distinct signed review) and at 79 (no executable pilot attested). The deterministic doctor returns `status: FAIL` for every proposal, as expected: the evidence bundle does not exist, Compute is FAIL, and the reviews are internal and unsigned. Dips are recorded.

| direction | proposal | contract | wave-2 → wave-3 | fix applied in wave 3 | reviewers' next highest-impact fix |
|---|---|---|---|---|---|
| `21-translation-supervised-sparse-indexer.md` | `research/proposals/2026-09-01-translation-supervised-sparse-indexer.md` | `experiments/architectures/translation-supervised-sparse-indexer.yaml` | 61 → **66** | Wave-3 (one repair): the judges' shared highest-impact identification fix, implemented as the union of both highest_impact_fix texts. (1) Both legs of the kill statistic xi are now non-literal: the mo … | Re-register the Phase-1 decision rule so the confirm and kill regions are disjoint at measured, not assumed, noise: (1) make five seeds [42,43,44,45,4 // Re-register T* and K2a on absolute cross-script selection against a fixed reference, and demote xi to a diagnostic. Concretely: (1) T* = argmax_T R_in … |
| `20-semantic-clock-gate-parity.md` | `research/proposals/2026-09-01-semantic-clock-gate-parity.md` | `experiments/architectures/semantic-clock-gate-parity.yaml` | 61 → **64** | Wave-3 repair = the judges' agreed highest-impact identification fix for C2 (union of both judges): (a) fertility decorrelated from resourcedness by adding rus (f 1.423, 6.9% CC), tur (1.429, 1.4%) an … | Make the attention-blind arm blind across the whole distractor prefix, not only at the query: run a second prefix pass in which the 8 full-attention l // Add a floor hold K7b symmetric to K7 (attention-blind EM(en) at d = 8 must exceed a pre-set value, e.g. 60, or the primary readout falls back to the a … |
| `22-translation-equivariant-state-writes.md` | `research/proposals/2026-09-01-translation-equivariant-state-writes.md` | `experiments/architectures/translation-equivariant-state-writes.yaml` | 57 → **63** | Prefix-identity leak in the supervised object (both judges' highest-impact fix, implemented as their union). The wave-2 loss acted on D = S(c+a) - S(c), which contains the content-independent term (pr … | Add a phase-1 sensitivity gate and a W-vs-P distinctness floor, both at near-zero marginal cost. (1) Attach TP-MQAR-v2 monolingual and cross-lingual E // Make the pilot executable and self-consistent in one versioned attempt: add `execution.runtime: docker-single-node-discovery-v1` and a compiled job ma … |
| `19-icl-rule-distillation-port.md` | `research/proposals/2026-09-01-icl-rule-distillation-port.md` | `experiments/architectures/icl-rule-distillation-port.yaml` | 62 → **57** | Wave-3 identification repair = the union of both wave-2 judges' highest_impact_fix, applied as one change and logged as wave 3 in the Iteration Log: (a) the teacher's own shuffled-label episodes enter … | Add one nested intermediate arm and make it the decisive control: R_GD-adaptive, an MLP of the same width and parameter count as R_theta, fed the same // Add an iso-parameter nonlinear GD-form control as the decisive control: same width-256 2-layer MLP, same compute, same 2:1 gold:shuffled episode mix a … |

Decision-bearing shortfalls flagged by at least one reviewer (ARS criterion-bound form, non-compensatory): Originality (all four — the deltas are recombinations, stated as such), Methodological Rigor (all four), Literature Integration (three), Evidence Sufficiency (three), Argument Coherence (three), Significance (two). Reviewer 2 on `translation-equivariant-state-writes` verified that the proposal's `submit_docker_research_job.py --dry-run` line fails because no compiled job manifest exists — a real blocker carried in `execution.blocked_by`.

Honest exit for this session: three waves ran; the best score moved 0 → 62 → 66; no proposal is pilot-ready. A 100 is unreachable inside one session by construction (it needs two provider-distinct Ed25519-signed reviews from the external trust store, a real model loop, a container smoke run, and a Slurm dry-run attested in a hashed evidence bundle). Wave 4 work orders are the reviewers' fixes above; the loop resumes with `uv run python scripts/research_direction_doctor.py <proposal>` after each wave and records every wave in `data/research-gauntlet/2026-09-01-frontier.jsonl`.
