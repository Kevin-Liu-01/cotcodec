# Wave 5 ledger — frontier gauntlet 2026-09-01

One implementation owner per direction turned the prose phase-0 pilot into executable, tested CPU code (NumPy/SciPy, torch never imported at module level), wired it into the contract's `reference_doctor`, ran it, and updated the proposal; two fresh reviewers then scored. All doctors pass their registered synthetic cases; every owner states what the doctor does **not** prove (no model, checkpoint, corpus, or GPU number).

| direction | wave 4 → wave 5 | doctor | tests | reviewers' binding defect |
|---|---:|---|---:|---|
| `icl-rule-distillation-port` | 65 → **65** | `scripts/run_icl_rule_distillation_doctor.py` | 17 | No reject-class defect (no direct-prior match, no fatal leakage, no safety red line). The most serious non-fatal defect, demonstrated by re-running the doctor's own objects: under the Gaussian-prior n … |
| `semantic-clock-gate-parity` | 63 → **64** | `scripts/run_semantic_clock_gate_parity_doctor.py (invocation: cd /Users/kevinliu/repos/cotcodec && uv run python scripts/run_semantic_clock_gate_parity_doctor.py --output data/results/semantic-clock-gate-parity/phase0-doctor.json; options --seed 42 --simulator-episodes 320 --dose-episodes 600 --resamples 500 --tally-seeds 5; exit 0 on PASS, 2 on FAIL; refuses to overwrite an existing receipt)` | 12 | None reject-class (no direct-prior match, no fatal leakage, no safety red line). The binding defects are (1) the registered pilot's decision-bearing measurement path does not exist as code on any regi … |
| `translation-supervised-sparse-indexer` | 62 → **63** | `scripts/run_translation_supervised_indexer_doctor.py` | 16 | The wave-5 claim this review was asked to check — "the executable doctor is real and passes" — holds only at the three registered seeds. Rerunning scripts/run_translation_supervised_indexer_doctor.py  … |
| `translation-equivariant-state-writes` | 60 → **62** | `scripts/run_translation_equivariant_state_writes_doctor.py` | 16 | Cap-class, not reject-class: the pilot that would produce a decisive number is still not executable. The CPU doctor is real (it implements the registered GDN recurrence, W/D/P, the fp64 identity, pref … |

Honest reviewer findings to carry forward: the sparse-indexer doctor passes at its three registered seeds but not under a wider seed sweep (a new versioned attempt must sweep seeds and report the failure rate, never retune thresholds); the icl-rule doctor's Gaussian-prior null exposes a co-condition that does not fail under the null (add registered negative-control gates that currently FAIL, as a new version); the clock-parity and state-writes decisive pilots are GPU screens on `qwen3.5-4b-base` that still lack a compiled discovery manifest (the reviewers name the template: `infra/slurm/host-single-node/qwen35-4b-competence-screen.yaml`, receipt and image ID now exist).

Score trajectories (lower reviewer total): sparse-indexer 61 → 66 → 62 → 63; clock-parity 61 → 64 → 63 → 64; state-writes 57 → 63 → 60 → 62; icl-rule 62 → 57 → 65 → 65. Best per wave: 66 (w3), 65 (w4), 65 (w5).

**Exit.** The gauntlet's fifth exit fires: under two points of gain across three waves (66 → 65 → 65). The loop stops for this session with the wave-5 work orders recorded. Resuming requires the decisive GPU screen to become executable (compiled manifest + fresh-job resume proof), not another prose wave.
