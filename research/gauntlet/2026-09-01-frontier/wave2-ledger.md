# Wave 2 ledger — frontier gauntlet 2026-09-01

Eight novelty-surviving candidates each received a single repair owner (work order: the wave-1 identification and feasibility objections; pilots re-based on named public license-cleared corpora with General Translation data as an optional upgrade; budgets re-derived from cited throughput with a 25% reserve). Each repaired candidate then faced the same refute-first triad (default `refuted=true`), blind closest-prior discrimination, and two independent judges. Scores are the lower of two judge totals and are capped at 89 because no provider-distinct signed review exists.

## Ranked (4)

| # | candidate | score | scope | pilot GPU-h | triad | blind discrimination | judges' shared fatal defect |
|---:|---|---:|---|---:|---|---|---|
| 1 | `icl-rule-distillation-port` | 62 | architecture-causal | 6 | nov=ok ide=R fea=ok | different mechanism, prior does not dominate | The primary endpoint r_TL = (gold-write gain minus shuffled-label-write gain) is not a matched placebo across the two arms it compares: R_theta is a nonlinear 1e5-parameter MLP trained only on coherent gold episodes, so  … |
| 2 | `semantic-clock-gate-parity` | 61 | architecture-causal | 4 | nov=ok ide=R fea=ok | different mechanism, prior does not dominate | C2, the only causal claim the pilot can actually test, is not identified as written. (1) On the Qwen3.5 tokenizer the eight high-fertility languages have log fertility correlated -0.85 (Pearson; -0.71 Spearman) with log  … |
| 3 | `translation-supervised-sparse-indexer` | 61 | attachment-capability | 4 | nov=ok ide=R fea=ok | different mechanism, prior does not dominate | Identification (refuter confidence 0.72, and I concur): the pre-registered counterfactual for every L_x claim is arm (b) = KL-to-head-sum, the weakest target aggregation in the class the candidate says it fixes. The de-d … |
| 4 | `translation-equivariant-state-writes` | 57 | architecture-causal | 3.2 | nov=ok ide=R fea=ok | different mechanism, prior does not dominate | The training objective leaks the shared prefix, so the headline contrast does not measure translation equivariance. With per-head scalar decay, D(a/c) = S(c+a) - S(c) contains the term (prod_t alpha_t) S(c) prod_t (I - b … |

## Killed in wave 2 (4)

| candidate | triad | dominant reason |
|---|---|---|
| `cross-family-adapter-port` | nov=ok ide=R fea=R | The wave-2 repair is a real improvement (iso-corpus Pile trio, teacher-relabel and KD controls, R^2 covariate), but the pilot as written still cannot attribute the predicted retention pattern to the stated mechanism (softmax coverage c, "no … |
| `commit-gated-observation-state` | nov=ok ide=R fea=R | The repaired pilot contains most of the right arms but its decision rule never uses the strongest one, and its headline contrast is a stub-induced train/test mismatch rather than a test of the recurrent mechanism.  1. Strongest control pres … |
| `frozen-reader-anchored-media` | nov=R ide=R fea=ok | No single paper found through 2026-09-01 that trains a discrete low-rate reasoning code against a frozen, weaker, pre-committed external reader on a 2-bit label subset and scores never-rewarded-variable legibility with bits-matched controls … |
| `interlingua-trace-codes` | nov=ok ide=R fea=R | Refuted as written: the phase-0 pilot (arms A/B/C, K1-K3) cannot attribute a Delta_L(A)-Delta_L(B) >= 5 result to "parallel translation data" rather than to (a) the training objective itself, (b) consistency regularization reaching the test … |

Owner-dropped: none. Full records (candidates, votes, discrimination, judge scorecards): `wave2-result.json`; repaired candidate notes: `wave2/`.
