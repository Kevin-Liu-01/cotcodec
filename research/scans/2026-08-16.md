# Frontier memory scan — 2026-08-16

## Decision

No new broad memory direction is promoted. Three bounded lifecycle candidates,
pinned Mnemosyne OSS
`a0e14243e04dbe3fc29287e58126ff5dc0e02b35`: it is a useful one-way
working-to-episodic consolidation control, not a demonstrated bidirectional
active/inactive pager, and Icarus
`6e348708dcddb7cf1ad47726cb287cd4c9183c40`: a manual archive/wiki floor,
and Palimpsest
`0f83e166b0512a5ca9f38c2559f68749b35e994d`: a pre-restart
bitemporal stale-state control, are now actor-blocked by reproduced lifecycle
falsifiers.

## Executed falsifier

The exact upstream source ran twice in clean, non-root, network-disabled,
read-only ARM64 Docker states. No model, embedding provider, secret, GPU, or
sudo was used. Both repetitions agreed:

- duplicate retry, session isolation, forced consolidation, and fresh-process
  episodic recall passed;
- consolidated memories left active context;
- recall did not reactivate them into active context;
- documented forget deleted the source row but left its episodic summary
  recallable and physically present in SQLite;
- no native session-scoped purge was available.

Terminal status:
`BLOCKED_CONSOLIDATED_FORGET_AND_NO_REACTIVATION`. H100 actor admission is
forbidden for this revision. This is lifecycle evidence only, with
`scientific_result=false` and `publication_ready=false`.

## Evidence

- Audit: `research/mnemosyne-lifecycle-audit-2026-08-16.md`
- Evidence: `research/evidence/memory/mnemosyne-one-way-consolidation-negative-v1.json`
- Evidence SHA-256: `3b516fa52f2e5144e30f18563d2f565a250fc939cd84f040974941ee66641587`
- Report SHA-256: `2479729d4eebea8dcd95605bd4fb9b79c8dd0f0894dfc482238782eea0b68bcc`
- Manifest SHA-256: `d75a443016a2e41b9087f9b2c73f76246ce6a667074e696e1d636ca0979298b0`

## Icarus manual-lifecycle falsifier

The exact upstream source also ran twice in clean, non-root,
network-disabled, read-only ARM64 Docker states with zero model, embedding,
API, GPU, or sudo use. Both repetitions reproduced explicit promotion from
working memory to a private archive and caller-selected shared wiki, agent
isolation, supersession, non-destructive rollback, and fresh-process restart.

Both repetitions also falsified admission:

- replaying `end_session` created another private summary and shared-wiki link;
- the public surface exposed no native delete, forget, or scoped purge API;
- all four private, shared, superseded, and replacement plaintext canaries
  remained resident after the purge probe.

Terminal status:
`BLOCKED_NON_IDEMPOTENT_PROMOTION_AND_NO_NATIVE_PURGE`. The upstream dependency
range separately resolved to incompatible MCP 2.0.0, producing 207 passes, 6
failures, and 39 skips. This remains lifecycle evidence only.

- Audit: `research/icarus-lifecycle-audit-2026-08-16.md`
- Evidence: `research/evidence/memory/icarus-manual-lifecycle-negative-v1.json`
- Evidence SHA-256: `9d476930a05dda1239b38bd35b71388eac3e0030a408a599b2d0b11052b9b6e4`
- Report SHA-256: `1dd616442c88ad962262d8e4b7f238bca0d2f99fff418725e1600e170d5372c1`
- Manifest SHA-256: `150979e9f4039b5d0386c1b334205da2808cff60036c0bf3d582479bbef88d3d`

## Next queue

Palimpsest has now completed the same bounded CPU admission lane twice. Before
restart, ordinary valid-time queries, a transaction-time cutoff, mixed
cardinality voting, and row-count idempotency passed. After restart, transaction
closures and per-key cardinality state were lost; the historical cutoff vanished
and continued state diverged from the uninterrupted branch. Logical correction
left the plaintext canary in SQLite and the public surface exposed no native
delete, forget, or scoped purge.

Terminal status:
`BLOCKED_BITEMPORAL_RESTART_AND_NO_NATIVE_PURGE`. The machine receipt is
`research/evidence/memory/palimpsest-bitemporal-negative-v1.json`
(`c0fa0f98...`), report `cd3cf86d...`, and manifest `2a168ddf...`.
This is lifecycle evidence only; H100 actor admission is forbidden for the
revision.

The next CPU adapter is intentionally unselected until the live portfolio is
re-ranked against mechanism novelty and testability. Only a candidate that
passes lifecycle, restart, lineage, purge, and residue gates may consume H100
actor budget. The larger all-SERVE Qwen3.6-35B wave remains blocked on a clean
source capsule, immutable image/SBOM, protected attestation, and restored
non-sudo Docker authority in the Slurm lane.

## LightMem2 context-paging falsifier

Pinned LightMem2 revision `dfc67e8bc9373ca5b31bb412298565c9d65b29b6`
ran twice in clean, non-root, network-disabled, read-only ARM64 Docker states.
Archive-before-stub and strict lower-level session lookup exist, but the actual
MCP recovery path searches all sessions under the shared state root. Both runs
recovered session B plaintext without a session parameter before and after
restart. Two same-millisecond archive writes also reused one filename, and the
native artifact-store surface exposes no scoped purge. The exact pin is
H100-blocked with status
`BLOCKED_CROSS_SESSION_DISCLOSURE_ARCHIVE_COLLISION_AND_NO_NATIVE_PURGE`.

- Audit: `research/lightmem2-context-paging-audit-2026-08-16.md`
- Evidence: `research/evidence/memory/lightmem2-context-paging-negative-v1.json`
- Evidence SHA-256: `1d02c379962b58ed584ad68d9844cba6a5bed9346871c6a12f06c861fa5cb102`

## Shodh overlapping-tier and restart falsifier

Pinned Shodh revision `98c6e4861847a76f75eb880acf9e145d30794a46`
also ran twice in the same zero-model CPU admission lane. Both runs showed that
a new Working record is already stored in RocksDB, while the process-local
Working cache disappears on restart and the durable record retains a stale
Working label. A 26-hour-old persisted Session record reopened with an empty
Session cache; real maintenance made zero promotions and left it stranded.
Public `forget(All)` returned two removals for one overlapping record. A raw
plaintext scan was negative before and after forget, so no physical-erasure
claim is made.

The upstream locked tiering suite passed 15/15 and persistence suite passed
20/20 in a separately built image, but neither suite tests active-cache
reconstruction or offline-aged promotion after a fresh process. The exact pin
is H100-blocked with status
`BLOCKED_OVERLAPPING_RESIDENCY_AND_RESTART_STRANDING`.

- Audit: `research/shodh-tier-admission-audit-2026-08-16.md`
- Evidence: `research/evidence/memory/shodh-tier-admission-negative-v1.json`
- Evidence SHA-256: `e8805c42e64847eb82858b09d7d56b83b7fd71afd8a1633d840afcf272f278ad`
- Report SHA-256: `0bf608d53af33557ee76da1c9c5086796218640753d8775e02fc8d89d17135b0`
- Manifest SHA-256: `e26146d5f490fd3154d70d0e98483e8da51121beabcc69edd6e5fb008a349eb0`

## Mnemon static-routing H100 falsifier

Mnemon core `88d2981` and `dsh-mnemon` `1889c68` ran twice in fresh non-root,
network-disabled, read-only ARM64 Docker states with no model, provider, GPU,
secret, or sudo use. Both runs reproduced isolated named databases, active-set
default-recall filtering, inactive-read rejection, targeted-write activation,
and registry persistence across restart. The same doctor also reproduced the
boundary: item forget is a soft delete that left its unique plaintext in
SQLite, while whole-space deletion removed only a non-final store.

That CPU result admitted one bounded actor falsifier. Slurm job 313 then ran
all 128 preregistered Qwen3.5-4B cases on one H100, and fresh-allocation job
315 reproduced every actor artifact byte-for-byte without regenerating cases.
No-memory scored 0.0 token F1; all-spaces, lexical routing, and oracle-space
each scored 1.0. Lexical routing therefore produced no quality lift over
all-spaces, and its 0.7815 prompt-token ratio failed the matched-budget gate.
Evidence is `research/evidence/memory/mnemon-h100-static-space-negative-v1.json`
(`e94b3ece...`), with status `MNEMON_STATIC_ROUTING_KILLED`.

## RecMem consolidation lifecycle falsifier

RecMem `a84252f` ran twice in clean non-root, network-disabled, read-only ARM64
Docker states with deterministic local storage, embedding, and operation test
doubles and zero provider/model calls. Both runs reproduced three admission
failures: an exact retry created a second durable raw record, the write that
triggered consolidation was missing from native `raw_ids` lineage, and an
injected replacement-embedding failure deleted the prior episode before the
replacement could be stored. Successful consolidation survived fresh-process
reopen, and conversation isolation passed.

The exact revision is blocked from an H100 quality actor. Evidence is
`research/evidence/memory/recmem-consolidation-negative-v1.json`
(`d870b8d1...`), with status
`BLOCKED_NON_IDEMPOTENT_WRITE_MERGE_DATA_LOSS_AND_INCOMPLETE_LINEAGE`.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 21 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`f5f1030321fa00f52df2952e4731b2261b55355b9bb99cc459f835b36eb46d31`.
None of these lifecycle results is a memory-quality result.

## TokenMizer checkpoint lifecycle falsifier

TokenMizer `131e3d1` / tree `cc5e934` ran twice in clean non-root,
network-disabled, read-only ARM64 Docker states. Both runs were byte-identical.
Normal checkpoint text, fresh-process resume, and session isolation passed.
Restart discarded process-local diff history, so the next diff falsely reported
old and new nodes as additions. Repeating one manual checkpoint created duplicate
durable rows. Corrupt-store recovery deleted and recreated the database empty,
losing both rows. No native scoped purge exists.

The upstream runtime also lazily fetches `o200k_base`; the discovery image had
to prefetch the hash-verified tokenizer asset before networkless execution.
Evidence is `research/evidence/memory/tokenmizer-checkpoint-negative-v1.json`
(`932bb2c0...`), with status
`TOKENMIZER_ACTIVE_INACTIVE_ADMISSION_KILLED`. This is checkpoint-lifecycle
evidence only: context-compaction quality was not evaluated, and this exact
revision is forbidden from the active/inactive H100 wave.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 22 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`6512868d68d03e9388dec29cc30f04f249a19022b8e79a24279865f671953b10`.

## TiMem core runtime falsifier

TiMem `6d279a5` / tree `24645b2` ran twice in clean non-root,
network-disabled, read-only ARM64 Docker states. The released Python tree
compiled, but three independent pre-model constructor paths failed: L1 used
its processor class as a fragment record, L2 passed unsupported fields to its
local session dataclass and returned `None`, and L5 omitted required
`updated_at` values from both the normal and fallback path.

Evidence is `research/evidence/memory/timem-core-runtime-negative-v1.json`
(`742f0f67...`), with status `TIMEM_CORE_RUNTIME_ADMISSION_KILLED`. This is
source-runtime evidence only. No hierarchy quality, graph retrieval, or
active/inactive residency effect was evaluated, and the exact revision is
forbidden from H100 actor work.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 23 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`995b9b54f693b2601e65ae313b8db3ba5606290350d28010824a28a6a6652638`.

## Mnemosyne Cognitive lifecycle falsifier

Mnemosyne Cognitive Memory OS `5506aae` / tree `d5cb986` ran twice in fresh,
non-root, internally networked Docker states with a digest-pinned Qdrant sidecar,
read-only roots, dropped capabilities, bounded resources, zero provider/model
calls, and no GPU access. The four committed upstream Vitest files passed 62/62
twice, but the native lifecycle doctor reproduced six actor-blocking failures.

Public dry-run consolidation mutated stale priority from 0.8 to 0.4; the next
call halved the same record again to 0.2. The record stayed in normal serving
search, so this is not an inactive residency transition. Public forget reported
success and hid its target from search, while the Qdrant point, deleted tombstone,
and exact plaintext canary survived a fresh database process. No native scoped
purge operation exists. Both clean repeats were byte-identical at the structured
initial and restart projections.

Evidence is
`research/evidence/memory/mnemosyne-cognitive-lifecycle-negative-v1.json`
(`6ee8209b...`), with status
`MNEMOSYNE_COGNITIVE_ACTIVE_INACTIVE_ADMISSION_KILLED`. This is negative
lifecycle evidence only; graph and memory quality were not tested, and this
revision is forbidden from H100 actor work.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 24 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`71b754cbf9a4f3177709e68de5f01c3f62e060facf17595b80bd025134195021`.

## MemForge fresh-install falsifier

MemForge `16e2f15` / tree `97411a5` was tested twice per lane in clean,
non-root, network-disabled, read-only ARM64 Docker states. The repository's
exact `postgres:16-alpine` Compose image stopped at canonical schema line 14
because it does not contain the required vector extension. A digest-pinned
pgvector control got past extension creation and then stopped at line 57,
where the schema creates an index on `warm_tier` before creating that table at
line 73. All four attempts exited 3 before initialization completed.

Evidence is `research/evidence/memory/memforge-fresh-install-negative-v1.json`
(`299d7b9d...`), with status `MEMFORGE_FRESH_INSTALL_ADMISSION_KILLED`.
No lifecycle or memory-quality mechanism ran; this exact revision is forbidden
from H100 actor work. A schema repair is a separate intervention, not a silent
continuation of this result.

The live ledger now binds 229 sources, 168 pinned-repository sources, three
local conformance results, and 25 reproduced negative findings. The
93-candidate portfolio retains its 108-H100-hour ceiling at matrix
`c6b4379860319bafe1a5b1f6d24461cf796fbc31e59eaa8f299dc265d319e16d`.

## MemoryBank corrected-decay H100 falsifier

The three frozen MemoryBank/SiliconFriend controls ran against Qwen3.5-4B in
network-disabled Docker under Slurm on one H100. Initial jobs 328-330 and exact
checkpoint resumes 333-334 completed the registered assignment-seed matrix.
Corrected decay beat the historical upstream-precedence expression by 10.97
served executable-success points, with a task-clustered bootstrap 95% interval
of `[6.15, 16.24]`.

The decisive control went the other way: corrected decay minus no decay was
-58.39 points, with interval `[-66.77, -50.00]`. Valid-action rate was 1.0 in
every cell and safety failures were zero. The corrected formula repairs the
upstream arithmetic bug on this synthetic panel, but decay itself removed
useful evidence. Larger-model scaling of this exact mechanism is killed.

Evidence is `research/evidence/memory/memorybank-h100-actor-v1.json`
(`bd069e22...`) with status
`MEMORYBANK_CORRECTED_DECAY_PASS_NO_DECAY_KILLS_SCALING`. The aggregate file
hash is `8a7e377b...` and its semantic root is `27044eb2...`. This remains a
dirty-tree, synthetic-panel discovery result, not an upstream MemoryBank or
paper reproduction and not publication-ready evidence.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 26 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`a7373a2d8b7b8a9b493d1028f57606d5fbcf22574332d60fb3ce297d625f058b`.

## LightMem exact-source offline-consolidation falsifier

Pinned LightMem revision `8fc9a9179f9170c4a40fc653fcb410375900f26e`
ran twice in fresh non-root, read-only, network-disabled ARM64 Docker states.
Both executions emitted the identical projection
`80a2b06c818ece9fce8319c0121d3e951b7469e456ed636b79d6d02f1aa72b56`.

The exact source reproduced a valid later-source-to-earlier-target
consolidation direction, but failed admission before a model-quality test:

- `online_update` is a no-op;
- default local Qdrant reopen removes an existing configured store, and the
  official offline script omits the persistence flag;
- the automatic trigger passes an unsupported keyword;
- consolidation changes payload text without re-embedding it;
- context-only retrieval still requires embedding components; and
- source-event lineage plus native scoped purge are absent.

The sealed receipt is
`research/evidence/memory/lightmem-offline-negative-v1.json` (`2c99c194...`)
with status
`BLOCKED_DESTRUCTIVE_DEFAULT_REOPEN_AND_CONSOLIDATION_CONTRACT_DRIFT`.
This is component/lifecycle evidence only, not a paper or memory-quality
reproduction. The exact revision was removed from the active/inactive H100
execution order.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 27 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`4a65532f1125f89123ae9f1b6d206f5ff468e421863a6fb4a0dad54a370ce233`.

## Memoria exact-source transactional-lifecycle falsifier

Memoria revision `efd3d6515969971dfa894737272b8317bcb643e7` ran twice in
fresh non-root ARM64 Docker states against digest-pinned MatrixOne. Both runs
forced two MatrixOne process restarts and emitted the same phase projection
`b5281d07d35d4bbdc5cb053d4a06cc3ea53026e93f4d7ed2c2ec55e909a06a33`.

Native snapshot, branch divergence, conflict-preserving merge, idempotent
re-merge, deletion, and restart persistence passed. Three admission blockers
also reproduced:

- a branch in legacy shared-database mode contained another user's rows;
- soft purge retained the inactive row after restart and the public API source
  reports a constant purge count; and
- snapshot restore is a non-atomic `DELETE` followed by `INSERT`.

The sealed receipt is
`research/evidence/memory/memoria-transactional-lifecycle-negative-v1.json`
(`92c8427e...`) with status
`BLOCKED_SHARED_TABLE_BRANCH_EXPOSURE_SOFT_PURGE_RESIDUE_AND_NONATOMIC_ROLLBACK`.
This is exact-source component/lifecycle evidence only. Multi-database
isolation, retrieval quality, active/inactive paging, paper results, and H100
actor quality were not tested; the exact revision is actor-blocked.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 28 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`76d85fbff35855fe1ebc216c3dcd85e8d82ee9b140f9109796a9de27fb052111`.

## Agent Recall exact-source scoped-lifecycle falsifier

Agent Recall revision `dcf21b5cc9691e1371299917e2e474fb82e07cab` ran twice in
fresh non-root, read-only, network-disabled ARM64 Docker states. Both executions
used a fresh named volume followed by a new container process and emitted the
same phase projection
`2fed18f7943ef6e96ce343ee665c31b90c03f1115eb6ddf0a807989abddcc5a3`.

Scope precedence, bitemporal correction history, and restart persistence passed.
Three lifecycle blockers also reproduced: a client-a bridge deleted one shared
entity carrying client-b state; a parent-scope change omitted descendant briefing
cache invalidation even though the child inherited the value; and observation
deletion retained the archived plaintext row in SQLite after restart. Neither
the store nor MCP bridge exposes a native scoped purge.

The sealed receipt is
`research/evidence/memory/agent-recall-scope-lifecycle-negative-v1.json`
(`c9695f9f...`) with status
`BLOCKED_CROSS_SCOPE_DESTRUCTIVE_DELETE_STALE_CHILD_BRIEFING_AND_SOFT_DELETE_RESIDUE`.
This is exact-source lifecycle/component evidence, not briefing quality,
retrieval quality, active/inactive paging, or a model result. The exact revision
is removed from the H100 execution order.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 29 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`ce3c1db622b4832f8efa8da013c67f6bab589b9cf60728e5ae3c677f9cbff4e5`.

## agenticow exact-source branch-lifecycle falsifier

agenticow revision `dd4f437b92d2dbbc1f40dfa00023eed6e9c3bd84` ran
twice in clean non-root, read-only, network-disabled ARM64 Docker states. Both
runs used a fresh volume plus a fresh process restart and emitted identical
semantic phase projection
`eeb24984a901d4bcb2982eab89af6c9a85c5a07ce8db6ce8ca640e5942709571`.

Branch and nested-fork isolation, checkpoint rollback, tombstone masking,
sibling visibility, restart persistence, promotion persistence, and repeated-
promotion logical idempotency passed. Two lifecycle blockers also reproduced:

- promotion silently overwrote a newer parent write without a conflict guard,
  and the lost update persisted after restart; and
- tombstoned plaintext remained in the persisted manifest after restart while
  the public API exposed no native branch-scoped physical purge or
  cryptographic erasure contract.

The sealed receipt is
`research/evidence/memory/agenticow-branch-lifecycle-negative-v1.json`
(`87b045a1...`) with status
`BLOCKED_BLIND_PROMOTION_LOST_UPDATE_TOMBSTONE_RESIDUE_AND_NO_SCOPED_PURGE`.
This is exact-source component/lifecycle evidence, not memory quality,
active/inactive paging, model-effect, or publication evidence. The revision is
removed from the H100 execution order.

The live ledger now binds 229 sources, 182 pinned repositories, three local
conformance results, and 31 reproduced negative findings. The 93-candidate
portfolio retains its 108-H100-hour ceiling at matrix
`4174f49e61b81429a4aa96efb177b6d4cde0eeb2968b706ede63bc679ef645f2`.
## Active Graph exact-source lifecycle result

Active Graph revision `8aedb186...` passed native parent/fork divergence,
nested-fork isolation, structural diff, retirement idempotency, and
fresh-process restart in two clean contained CPU repetitions. It failed the
registered persistent-memory erasure boundary: upstream explicitly implements
retention as an archive tier with never-deletion semantics, the rejected run's
metadata and plaintext survived restart in the same SQLite database, and there
is no native run-scoped purge surface. The exact revision is therefore removed
from H100 actor execution with terminal status
`BLOCKED_ARCHIVE_ONLY_RETENTION_NO_SCOPED_PURGE_AND_SHARED_DB_ERASURE`.
This does not invalidate Active Graph as an event-sourced fork/replay substrate;
it invalidates its admission as a user-scoped persistent-memory system under
the current lifecycle contract.
