# Committed node contract read by scripts/orx_run.py — the single fixed run
# command of the OpenResearch experiment tree. Vary THIS FILE on a node's
# branch; never the run command. Two kinds are admissible:
#   kind: cpu-doctor     -> doctor: scripts/run_<name>_doctor.py, optional args (no --output)
#   kind: slurm-manifest -> manifest: experiments/<...>.yaml submitted only via
#                           scripts/submit_docker_research_job.py (dry-run, test-only, submit)
kind: cpu-doctor
direction: openai-daybreak-blue-capability
doctor: scripts/run_openai_daybreak_blue_doctor.py
args: []
